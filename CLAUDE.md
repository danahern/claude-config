# Claude Configuration

This repository contains the configuration for a Claude assistant.

## Structure

- `settings.json` - Main Claude Code settings
- `commands/` - Custom slash commands
- `hooks/` - Event hooks configuration

## Usage

This configuration can be used by symlinking or copying to `~/.claude/` or project-specific `.claude/` directories.
